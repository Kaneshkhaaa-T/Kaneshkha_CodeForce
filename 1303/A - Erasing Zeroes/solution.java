import java.util.*; public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int t = sc.nextInt();        sc.nextLine(); // consume newline         while (t-- > 0) {            String s = sc.nextLine();             int firstOne = -1;            int lastOne = -1;             // find first and last occurrence of '1'            for (int i = 0; i < s.length(); i++) {                if (s.charAt(i) == '1') {                    if (firstOne == -1)                        firstOne = i;                    lastOne = i;                }            }              if (firstOne == -1) {                System.out.println(0);                continue;            }             int countZero = 0;             // count zeros between firstOne and lastOne            for (int i = firstOne; i <= lastOne; i++) {                if (s.charAt(i) == '0') {                    countZero++;                }            }             System.out.println(countZero);        }        sc.close();    }}